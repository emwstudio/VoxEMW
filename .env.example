# ---- LLM API key（必填）----
# configs/alerter.yaml 里 blocks.llm.api_key_env 指向这个名字；server 从环境读取
# （兜底变量名 LLM_API_KEY），缺失会直接报错。
# DeepSeek key 在 https://platform.deepseek.com 申请
DEEPSEEK_API_KEY=

# ---- 可选：配置路径（默认 configs/alerter.yaml）----
# VOXEMW_CONFIG=configs/alerter.yaml
